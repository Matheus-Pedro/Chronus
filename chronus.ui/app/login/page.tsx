import { LoginForm } from "@/components/auth/login-form";

export const metadata = {
  title: "Login - Chronus",
  description: "Acesse sua conta no Chronus para gerenciar suas tarefas",
};

export default function LoginPage() {
  return <LoginForm />;
} 